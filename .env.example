# Jarvis 环境配置示例
# 复制此文件为 .env 并填入实际值

# 日志级别
LOG_LEVEL=INFO

# 记忆存储路径
MEMORY_ROOT=./memory

# 审计日志路径
AUDIT_LOG_PATH=./memory/raw_logs/audit.log.jsonl

# MCP 服务器配置（可选）
# MCP_SERVER_URL=http://localhost:8000

# ============================================
# LLM 配置（可选）
# ============================================

# LLM 提供商选择（none|openai|gemini）
# none: 不使用 LLM，完全走规则路由和规划
# openai: 使用 OpenAI 兼容接口（包括国内兼容服务）
# gemini: 使用 Google Gemini API
LLM_PROVIDER=none

# LLM 功能开关（0=关闭，1=开启）
# LLM_ENABLE_ROUTER: 启用 LLM 路由（规则优先，仅在不确定时使用 LLM）
LLM_ENABLE_ROUTER=0
# LLM_ENABLE_PLANNER: 启用 LLM 规划（生成 JSON 步骤结构）
LLM_ENABLE_PLANNER=0

# LLM 请求超时时间（秒）
LLM_TIMEOUT_SECONDS=30

# OpenAI 兼容接口配置（包括国内兼容服务）
# OPENAI_API_KEY: API 密钥（必填，如果使用 OpenAI 提供商）
OPENAI_API_KEY=
# OPENAI_BASE_URL: API 基础地址（默认 https://api.openai.com，国内兼容服务可修改此处）
OPENAI_BASE_URL=https://api.openai.com
# OPENAI_MODEL: 模型名称（可选，默认 gpt-4.1-mini）
OPENAI_MODEL=

# Gemini API 配置
# GEMINI_API_KEY: API 密钥（必填，如果使用 Gemini 提供商）
GEMINI_API_KEY=
# GEMINI_MODEL: 模型名称（可选，默认 gemini-1.5-flash）
GEMINI_MODEL=
