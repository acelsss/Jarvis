# Preferences v0.1
# 系统偏好设置

defaults:
  log_level: "INFO"
  timeout_seconds: 300
  max_retries: 3

ui:
  show_progress: true
  verbose_output: false

memory:
  retention_days: 30
  auto_cleanup: true

sandbox:
  sandbox_root: "./sandbox"  # 沙箱根目录，所有文件操作必须在此目录下
