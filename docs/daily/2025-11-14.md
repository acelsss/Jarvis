# 2025-11-14 开发日志（REPL 调试输出增强）

---

## Update 1：REPL 调试输出增强

### 目的与要点

- 在 REPL（`scripts/chat.py`）中增加调试可视化输出，方便在终端查看：
  - 每一轮命中的记忆内容和分数
  - 发给 DeepSeek 的最终 messages（prompt）内容
- 不改动现有业务逻辑和调用顺序，仅增加日志输出
- 确保不打印任何敏感信息（API key、headers 等）

### 变更文件

- `scripts/chat.py` - 增加两处调试输出：
  1. 在 `search()` 完成后，输出命中记忆的详细信息（序号、分数、内容截断）
  2. 在调用 `chat_completion()` 前，输出完整的 messages payload（JSON 格式）

### 使用示例

**终端输出示例：**

```
You> 我喜欢手冲咖啡

[debug][memory] hits=2
[debug][memory] #1 score=0.873 content=我家有一只11岁的柯基，名字叫丁丁，它非常喜欢在公园里奔跑...
[debug][memory] #2 score=0.420 content=为了记录：我住在26楼，每天早上都会喝一杯手冲咖啡，不加糖...

[debug][llm] messages payload:
[
  {
    "role": "system",
    "content": "你是一个智能助手 Jarvis。用户会向你提问或分享信息。\n请结合用户的本轮发言和相关记忆（如果有）给出简洁、有用的回复。\n如果相关记忆为空，仅基于用户本轮发言作答。"
  },
  {
    "role": "user",
    "content": "用户本轮发言：\n我喜欢手冲咖啡\n\n相关记忆（可能为0~K条）：\n1) [note] 我家有一只11岁的柯基，名字叫丁丁...（2025-11-10）\n2) [note] 为了记录：我住在26楼，每天早上都会喝一杯手冲咖啡，不加糖...（2025-11-10）\n\n请基于以上信息简洁作答。"
  }
]

Jarvis> 根据您的记忆，您每天早上都会喝一杯手冲咖啡，不加糖。看来您对手冲咖啡有特别的喜好。

[store: ok] [search: hits=2] [llm: used]
```

**无命中时的输出：**

```
You> 今天天气真好

[debug][memory] no hits

[debug][llm] messages payload:
[
  {
    "role": "system",
    "content": "你是一个智能助手 Jarvis。用户会向你提问或分享信息。\n请结合用户的本轮发言和相关记忆（如果有）给出简洁、有用的回复。\n如果相关记忆为空，仅基于用户本轮发言作答。"
  },
  {
    "role": "user",
    "content": "用户本轮发言：\n今天天气真好\n\n请基于以上信息简洁作答。"
  }
]

Jarvis> 是的，今天天气确实不错！

[store: ok] [search: hits=0] [llm: used]
```

### 实现细节

**记忆命中调试输出：**
- 使用 `log.info()` 输出，级别为 INFO
- 格式：`[debug][memory] hits=N` 或 `[debug][memory] no hits`
- 每条记忆输出：`[debug][memory] #序号 score=分数 content=内容（截断至120字符）`
- score 保留 3 位小数，None 时显示为 "None"
- content 超过 120 字符时自动截断并添加 "..."

**LLM prompt 调试输出：**
- 在调用 `chat_completion()` 前构造 messages（与函数内部逻辑一致）
- 使用 `json.dumps(messages, ensure_ascii=False, indent=2)` 格式化输出
- 格式：`[debug][llm] messages payload:\n{JSON}`
- 仅打印 messages 列表（role + content），不包含任何敏感信息

### 验证要点

- REPL 能正常启动
- 输入一句话时，终端中能看到：
  - `[debug][memory] ...`（命中列表或 no hits）
  - `[debug][llm] messages payload: ...`（完整 prompt JSON）
- 原有的 `[store/search/llm]` 摘要输出保持不变
- 不打印任何敏感信息（API key、URL、headers 等）

---

## Update 2：完善记忆读写逻辑重构

### 目的与要点

- 完善「Jarvis ↔ OpenMemory」的记忆读写逻辑
- 重构大模型提示词构造部分，使其**非常清晰、集中、易于后期修改**
- 实现智能记忆策略：不是所有话都要记，由 LLM 判断是否值得长期记忆
- 打通时间戳字段，支持去重状态显示

### 变更文件

**新增模块：**
- `core/memory/classifier.py` - 消息分类模块（EXPLICIT_STORE / NEVER_STORE / CANDIDATE）
- `core/llm/prompt_builder.py` - LLM 提示词构造模块（集中、清晰、易改）
- `core/llm/response_parser.py` - LLM 返回解析模块（防御性解析）
- `core/memory/metadata_builder.py` - 记忆元数据构建模块（合并系统级和 LLM 提供的 tags/metadata）

**修改文件：**
- `scripts/chat.py` - 重构主流程（分类→搜索→LLM→落盘）
- `core/memory/client.py` - 修改 `search()` 解析时间戳，修改 `store()` 返回 `StoreResult`
- `core/memory/types.py` - 添加 `StoreResult`，扩展 `SearchResult` 和 `make_memory_item`
- `core/llm/deepseek.py` - 简化接口，接受已格式化的 `user_prompt`
- `scripts/memory_search.py` - 更新时间戳显示
- `utils/config.py` - 默认 `user_id` 设为 "lushan"

### 核心功能

**1. 消息分类（MemoryStoreDecision）**
- `EXPLICIT_STORE`: 显式要求记录（"为了记录：""帮我记一下"等）
- `NEVER_STORE`: 明显不需要记录（debug 命令、一次性问句）
- `CANDIDATE`: 不确定，由 LLM 判断

**2. LLM 提示词构造（集中、清晰、易改）**
- 系统 prompt 定义在 `BASE_SYSTEM_PROMPT` 常量中，包含完整的 JSON schema 说明
- 根据 policy 动态添加策略说明
- 用户 prompt 清晰组织：相关记忆（带时间）→ 用户发言 → 指令

**3. LLM 返回解析（防御性）**
- 支持从 markdown 代码块中提取 JSON
- JSON 解析失败时降级为原始文本
- 验证 content 长度、tags/metadata 格式

**4. 时间戳打通**
- `MemoryClient.search()` 解析 `last_seen_at` / `created_at`，格式化为 `YYYY-MM-DD HH:MM`
- `SearchResult.timestamp` 存储格式化时间字符串
- `scripts/memory_search.py` 和 REPL debug 输出显示时间

**5. 记忆写入（LLM 建议 + 去重状态）**
- 根据 LLM 返回的 `memories_to_store` 写入
- 合并系统级 tags（jarvis, user:xxx, channel:xxx）和 LLM 提供的 tags
- 合并系统级 metadata（source, autolog, recorded_at）和 LLM 提供的 metadata
- 解析 `deduplicated` 字段，显示 `[store: new]` 或 `[store: dedup]`
- EXPLICIT_STORE 的 fallback：JSON 解析失败时直接存储用户输入（去掉前缀）

### LLM 系统 Prompt 最终版本

```python
BASE_SYSTEM_PROMPT = """你是 Jarvis 的对话与记忆助手。每次对话你要做两件事：

1. 回复用户（reply 字段）
2. 如果本次对话中有值得长期记忆的事实或偏好，提炼为若干条记忆（memories_to_store 字段）

你必须返回一个严格的 JSON 对象，格式如下：

{
  "reply": "<你要回复给用户的话>",
  "memories_to_store": [
    {
      "content": "<适合写入记忆库的单句事实>",
      "tags": ["可选的标签数组"],
      "metadata": {
        "importance": 0.1~1.0,
        "event_date": "YYYY-MM-DD，可选",
        "valid_from": "ISO 时间字符串，可选",
        "valid_to": "ISO 时间字符串或 null，可选",
        "...": "你认为有用的其他字段"
      }
    }
  ]
}

注意：
- 一定要返回合法 JSON，不要包含多余的注释或解释文字。
- reply 应该是自然语言完整回复。
- memories_to_store 用于长期存储，只有在确实值得长期记忆时才添加。
- content 应该是简洁、独立的单句事实，避免冗长描述。
- tags 用于分类和检索，选择 1-5 个相关标签即可。
- metadata.importance 表示这条记忆的重要性（0.1=低，1.0=高）。
"""
```

### 时序说明

**完整流程：用户输入 → 分类 → 搜索 → 调 LLM → 落盘**

1. **用户输入**：读取用户输入文本
2. **消息分类**：调用 `classify_message_for_memory()` 判断策略（EXPLICIT_STORE / NEVER_STORE / CANDIDATE）
3. **检索记忆**：调用 `MemoryClient.search()` 检索相关记忆，解析时间戳
4. **构造提示词**：调用 `build_deepseek_prompt()` 构造系统 prompt 和用户 prompt
5. **调用 LLM**：调用 `chat_completion()` 获取原始 JSON 响应
6. **解析响应**：调用 `parse_deepseek_response()` 解析 JSON，提取 `reply` 和 `memories_to_store`
7. **写入记忆**：
   - 对每条 `memories_to_store`：
     - 调用 `build_final_tags_and_metadata()` 合并 tags/metadata
     - 调用 `make_memory_item()` 创建 `MemoryItem`
     - 调用 `MemoryClient.store()` 写入，获取 `StoreResult`
     - 根据 `status`（new/dedup）记录状态
   - EXPLICIT_STORE fallback：如果 JSON 解析失败或 `memories_to_store` 为空，直接存储用户输入（去掉前缀）
   - NEVER_STORE：忽略 LLM 返回的 `memories_to_store`
8. **输出摘要**：显示 `[store: new=2, dedup=1] [search: hits=3] [llm: used]`

### 配置项

**新增/修改配置：**
- `OM_USER_ID`：默认值 "lushan"（可通过环境变量覆盖）
- `channel`：固定为 "repl"（在 `scripts/chat.py` 中）

**使用方式：**
```bash
# 设置用户 ID
export OM_USER_ID=your_user_id

# 运行 REPL
python -m scripts.chat
```

### 验证要点

- REPL 能正常启动
- 消息分类正确（EXPLICIT_STORE / NEVER_STORE / CANDIDATE）
- 时间戳正确显示（YYYY-MM-DD HH:MM 格式）
- LLM 返回的 JSON 正确解析
- 记忆写入成功，去重状态正确显示（new/dedup）
- EXPLICIT_STORE fallback 正常工作
- 所有日志使用 `[Jarvis][xxx]` 前缀风格

---

### 工作概览

今天（2025-11-14）完成了两个主要更新，聚焦于 **REPL 调试可视化** 和 **记忆读写逻辑重构**，显著提升了 Jarvis 的记忆管理能力和开发调试体验。

### 主要成果

**1. REPL 调试输出增强（Update 1）**
- 在 REPL 中增加了记忆命中调试输出，显示每条记忆的分数、类型、时间戳和内容
- 增加了 LLM prompt 调试输出，完整展示发送给 DeepSeek 的 messages payload
- 所有调试输出使用统一的日志格式，不泄露敏感信息

**2. 记忆读写逻辑重构（Update 2）**
- 实现了智能消息分类（EXPLICIT_STORE / NEVER_STORE / CANDIDATE）
- 重构了 LLM 提示词构造逻辑，使其**集中、清晰、易于修改**
- 实现了基于 LLM 建议的记忆写入机制，支持去重状态显示
- 打通了时间戳字段，统一格式化为 `YYYY-MM-DD HH:MM`
- 实现了系统级 tags/metadata 与 LLM 提供的 tags/metadata 的自动合并

### 代码统计

**新增文件（4个）：**
- `core/memory/classifier.py` (102 行) - 消息分类模块
- `core/llm/prompt_builder.py` (177 行) - LLM 提示词构造模块
- `core/llm/response_parser.py` (124 行) - LLM 返回解析模块
- `core/memory/metadata_builder.py` (70 行) - 元数据构建模块

**修改文件（7个）：**
- `scripts/chat.py` - 重构主流程（+237/-105 行）
- `core/memory/client.py` - 时间戳解析和 StoreResult 支持（+115/-28 行）
- `core/memory/types.py` - 添加 StoreResult，扩展数据结构（+16/-1 行）
- `core/llm/deepseek.py` - 简化接口（-28 行）
- `scripts/memory_search.py` - 时间戳显示（+5 行）
- `utils/config.py` - 默认 user_id（+2 行）
- `docs/daily/2025-11-14.md` - 开发日志（+236 行）

**总计：** 11 个文件变更，+1007 行，-105 行

### 核心架构改进

**1. 提示词构造模块化**
- 所有 LLM 提示词构造逻辑集中在 `core/llm/prompt_builder.py`
- `BASE_SYSTEM_PROMPT` 作为常量定义，包含完整的 JSON schema 说明
- 根据 policy 动态添加策略说明，使 LLM 行为更可控
- 用户 prompt 结构清晰：相关记忆（带时间）→ 用户发言 → 指令

**2. 消息分类策略**
- 实现了基于规则的快速分类（EXPLICIT_STORE / NEVER_STORE）
- 不确定的情况交由 LLM 判断（CANDIDATE）
- 分类逻辑独立、无副作用、易于扩展

**3. 防御性解析**
- LLM 返回解析支持从 markdown 代码块中提取 JSON
- JSON 解析失败时自动降级，不中断主流程
- 验证 content 长度、tags/metadata 格式，确保数据质量

**4. 时间戳统一处理**
- `MemoryClient.search()` 自动解析 `last_seen_at` / `created_at`
- 统一格式化为 `YYYY-MM-DD HH:MM` 格式
- 在 REPL debug 输出和 CLI 工具中一致显示

**5. 记忆写入机制**
- 根据 LLM 返回的 `memories_to_store` 写入，而非直接存储用户原话
- 自动合并系统级 tags（jarvis, user:xxx, channel:xxx）和 LLM 提供的 tags
- 支持去重状态显示（new/dedup），基于 OpenMemory 返回的 `deduplicated` 字段
- EXPLICIT_STORE 策略提供 fallback 机制，确保显式要求记录时不会失败

### LLM 系统 Prompt 最终版本

系统 prompt 已优化，包含更详细的 JSON schema 说明和行为约束：

```python
BASE_SYSTEM_PROMPT = """
你是 Jarvis 的对话与记忆助手。每次对话你要同时完成两件事：

1. 回复用户（字段：reply）
2. 如果本次对话中出现了值得长期记忆的内容，帮用户提炼成若干条"记忆项"（字段：memories_to_store）

你必须返回一个【严格的 JSON 对象】，格式如下：

{
  "reply": "<你要回复给用户的话>",
  "memories_to_store": [
    {
      "content": "<适合写入记忆库的单句事实>",
      "tags": ["可选的标签数组"],
      "metadata": {
        "importance": 0.1~1.0,
        "event_date": "YYYY-MM-DD，可选",
        "valid_from": "ISO 时间字符串，可选",
        "valid_to": "ISO 时间字符串或 null，可选",
        "...": "你认为有用的其他字段"
      }
    }
  ]
}

要求说明：
- 一定要返回合法 JSON：不能包含注释、额外说明文字或多余的 key
- reply：是给用户看的自然语言完整回复，可以包含多句
- memories_to_store：只在"确实值得长期记忆"的时候才添加
- content：必须是单句、清晰、独立的事实陈述
- tags：建议选择 1~5 个简短、稳定的标签
- metadata：importance 表示重要性（0.1=很低，1.0=非常重要）

关于"当前记忆策略（policy）"：
- EXPLICIT_STORE：用户显式要求记录，你【至少】应该返回一条 memories_to_store
- NEVER_STORE：明显的一次性命令或问题，通常让 memories_to_store 为空数组
- CANDIDATE：不确定是否要记，你需要自行判断

请严格遵守以上 JSON 格式和行为约束。
"""
```

### 完整流程

**用户输入 → 分类 → 搜索 → 调 LLM → 落盘**

1. **用户输入**：读取用户输入文本
2. **消息分类**：`classify_message_for_memory()` → EXPLICIT_STORE / NEVER_STORE / CANDIDATE
3. **检索记忆**：`MemoryClient.search()` → 解析时间戳（`last_seen_at` / `created_at` → `YYYY-MM-DD HH:MM`）
4. **构造提示词**：`build_deepseek_prompt()` → 系统 prompt + 用户 prompt（集中、清晰）
5. **调用 LLM**：`chat_completion()` → 原始 JSON 响应
6. **解析响应**：`parse_deepseek_response()` → `(reply, memories_to_store)`（防御性解析）
7. **写入记忆**：
   - 对每条 `memories_to_store`：
     - `build_final_tags_and_metadata()` → 合并系统级和 LLM 提供的 tags/metadata
     - `make_memory_item()` → 创建 `MemoryItem`
     - `MemoryClient.store()` → 写入，获取 `StoreResult`
     - 根据 `status`（new/dedup）记录状态
   - EXPLICIT_STORE fallback：JSON 解析失败时直接存储用户输入（去掉前缀）
   - NEVER_STORE：忽略 LLM 返回的 `memories_to_store`
8. **输出摘要**：`[store: new=2, dedup=1] [search: hits=3] [llm: used]`

### 配置变更

- `OM_USER_ID`：默认值从 `None` 改为 `"lushan"`（可通过环境变量 `OM_USER_ID` 覆盖）
- `channel`：在 REPL 中固定为 `"repl"`

### 技术亮点

1. **提示词代码集中化**：所有提示词构造逻辑集中在 `core/llm/prompt_builder.py`，未来只需修改一处即可调整 LLM 行为
2. **防御性编程**：LLM 返回解析、时间戳解析、记忆写入都有完善的异常处理和降级机制
3. **日志风格统一**：所有新增日志使用 `[Jarvis][xxx]` 前缀，保持项目风格一致
4. **时间戳打通**：从 OpenMemory 返回的时间戳统一格式化，在 REPL 和 CLI 中一致显示
5. **智能记忆策略**：不是所有话都记录，由消息分类和 LLM 共同判断是否值得长期记忆

### 后续工作建议

1. **提示词优化**：根据实际使用效果，调整 `BASE_SYSTEM_PROMPT` 中的要求和示例
2. **分类规则扩展**：根据实际使用场景，扩展 `classify_message_for_memory()` 的规则
3. **标签体系**：建立统一的标签分类体系，指导 LLM 生成更规范的 tags
4. **测试覆盖**：为新增模块编写单元测试，确保防御性解析和异常处理正确
5. **性能优化**：如果记忆写入成为瓶颈，考虑批量写入或异步处理

### 提交记录

- `b977d3b` - `chore(chat): 增加 REPL 调试输出（记忆命中 + LLM prompt）（Update 1, 2025-11-14）`
- `449bdf2` - `feat(memory): 完善记忆读写逻辑重构（分类→LLM提示词→解析→落盘）（Update 2, 2025-11-14）`

---

